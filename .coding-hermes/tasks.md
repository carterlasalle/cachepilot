# CachePilot — Task Board

> Foreman: deepseek-v4-flash @ openrouter | DuckBrain: cachepilot

Cost-aware KV-cache lease optimization + nonblocking long-task runtime for
stock Hermes Agent. Spec-complete: `docs/PRD.md` (169 sections) is the
authoritative spec. Implementation follows the PRD's phase sequence
(§127–139), one PR = one phase. Read `AGENTS.md` + the relevant `docs/`
before each phase. Follow AGENTS.md invariants + PR scope rules; bridge
every commit to a `gitreins task complete`.

## Active

| ID | Task | Pri | Cpx | Deps | Tags | Model | Reasoning | Fallback |
|----|------|-----|-----|------|------|-------|-----------|----------|
| E2E-001 | E2E Testing Tick (self-improving loop) — spawn Luna (browser/screenshots) or Step 3.7 Flash (CLI/API). Deploy/build, Playwright, screenshots, endpoints, console. → e2e-output/tasks.md → inject into board. Every 5-10 ticks. Run 1 (2026-08-13, e838b28): 463 pytest pass, smoke 52/52, yarn build ✓, relay pass-through ✓, 4 findings → E2E-002..005. Run 2 (2026-08-13, 3300078, browser/Luna variant): console clean, visual at 1280/768/320px, live polling ✓, read-only contract ✓, 1 finding → E2E-006 (fixed 1095a97, judge PASS e210e179). Run 3 (2026-08-15, 064c098, CLI/API): 482 pytest, smoke ✓, all gates green; E2E-002..006 re-verified FIXED; 1 new finding → E2E-007. Run 4 (2026-08-15, 42249bb via E2E-007 fix): re-verified 405 contract exhaustive. Run 5 (2026-08-15, this tick, CLI/API): 482 pytest, ruff/mypy, yarn build (43 modules), smoke ✓; E2E-002..007 re-verified FIXED; 1 new finding → E2E-008 (corrupt-DB contract). Run 6 (2026-08-15, 449c76b via E2E-008 fix): re-verified corrupt-DB contract closed — all 8 CLI read commands + /api/* return honest empty on a corrupt/non-SQLite DB. Run 7 (2026-08-16, this tick, CLI/API): 482 pytest, ruff, yarn build (43 modules), smoke ✓; E2E-002..008 re-verified FIXED; 1 new finding → E2E-009 (wrong-schema SQLite crashes CLI + 500 on dashboard). Run 8 (2026-08-16, 2a56880 via E2E-009 fix): E2E-009 wrong-schema contract re-verified CLOSED (28 smoke asserts). Next run: every 5-10 ticks. | High | 4±1 | P12 | ++testing, +browser, +vision | GPT-5.6 Luna | Visual verification | Step 3.7 Flash |

## Completed

| ID | Task | Pri | Cpx | Commit | Model |
|----|------|-----|-----|--------|-------|
| E2E-008 | Corrupt/unreadable telemetry DB → honest empty store — both openers probe with PRAGMA quick_check on a scratch mode=ro conn (`_is_readable_sqlite()` in churn.py + server.py); present-but-corrupt/non-SQLite `--db` file treated exactly like missing (honest empty output, stderr notice naming path, exit 0, no traceback; dashboard `/api/*` return 200 empty JSON, no 500); smoke_test.py corrupt-DB case (exit 0, 200 empty JSON, stderr notice, no sqlite traceback); docs/dashboard.md contract updated; 482 pytest, ruff/mypy clean, smoke PASS (judge PASS 772922d8; spurious FAIL 6d3146ea was ruff-not-found PATH, pitfall #13) | Low | 2±1 | 449c76b | DS-V4-Flash |
| E2E-009 | Wrong-schema SQLite telemetry DB → honest empty store (E2E-008 contract continuity) — `_is_readable_sqlite()` in churn.py + server.py now ALSO verifies expected schema: after PRAGMA quick_check, counts expected CachePilot tables (request_events/churn_events/leases/provider_profiles/ttl_observations/route_events) via sqlite_master on the scratch mode=ro conn; absent/unrelated schema → probe False → treated exactly like missing/corrupt; `_print_empty_store_notice(path)` (names path + "corrupt, not SQLite, or lacks the expected telemetry schema … treating it as an empty store"); cmd_churn/cmd_explain_miss wrap SELECTs in try/except sqlite3.Error → notice + honest empty + exit 0 (defensive layer for probe/read races); server.py `_api` catches sqlite3.Error → rebuild empty → HTTP 200 empty JSON, never 500; smoke_test.py wrong-schema case (CREATE TABLE unrelated) — all 8 CLI read commands exit 0 + stderr names path + no "no such table"/traceback, dashboard /api/leases,/api/status,/api/costs 200 empty JSON (28 asserts); docs/dashboard.md wrong-schema bullet; 482 pytest, ruff/mypy clean, smoke PASS (judge PASS 1a96c257) | Low | 2±1 | 2a56880 | DS-V4-Flash |
| E2E-006 | 320px mobile layout — `@media (max-width: 768px)` collapses the 230px sidebar into a horizontal scrollable top nav (`.app` → 1fr, `.sidebar` → row, brand-sub/sidebar-foot hidden), reflows stat-grid/layer-groups/bar-row to single column, topbar wraps + reduced padding, empty-state wraps (white-space normal, word-break); desktop ≥768px untouched; CSS-only, no TS/backend changes; yarn build ✓ (43 modules), ruff/mypy clean (judge PASS e210e179) | Medium | 3±1 | 1095a97 | Hy3 |
| E2E-007 | Dashboard backend uniform 405 contract — do_OPTIONS/do_TRACE route through _write_refused() (405 JSON read-only refusal; send_json/send helpers body-aware); do_HEAD routes _write_refused(body=False): 405 status + JSON content-type headers, NO response body (HTTP HEAD semantics); smoke_test write-refusal loop extended 4→7 methods (POST/PUT/DELETE/PATCH/OPTIONS/TRACE/HEAD, HEAD uses data=b\"\" asserts empty body); docs/dashboard.md API table + Verification now uniformly state ALL non-GET methods refused with machine-readable JSON 405; GET read path/schemas untouched; pytest 482, ruff/mypy clean, smoke PASS (judge PASS 62a94533) | Low | 2±1 | 42249bb | DS-V4-Flash |
| E2E-005 | Route-change counters disambiguated — `cachepilot status` now labels the churn-attributed counter "route-change churn events" + footnote (churn_events.route_changed) vs `cachepilot routes` "route switches" (all route_events rows, PRD §25 vs §72 deliberately different); dashboard StatusView card label renamed; docs/dashboard.md documents route_changes vs route_switches; machine JSON field names unchanged (server.py/types.ts/smoke untouched); pytest 482, ruff/mypy clean, smoke + yarn build green (judge PASS a9535286) | Low | 2±1 | ba93d89 | DS-V4-Flash |
| E2E-004 | CLI reads open telemetry DB read-only — TelemetryStore gains `read_only` open mode (sqlite mode=ro URI, no mkdir/schema/migrations, mirrors ReadOnlyTelemetryStore); all 8 CLI read commands open read-only via shared `open_read_only_store()`; missing `--db`/`CACHEPILOT_TELEMETRY_DB` path is NEVER created (no file, no parent dir) — stderr notice names the path + "CLI reads are read-only; the relay creates the DB on first write" + honest empty output, exit 0; docs/dashboard.md read-only guarantees updated; pytest 482 (+13), ruff/mypy clean (judge PASS 39efcbc4) | Low | 3±1 | 93d7a32 | DS-V4-Flash |
| E2E-003 | Dashboard backend write-refusal contract — do_PUT/do_DELETE/do_PATCH now answer JSON 405 "read-only (GET only)" via shared _write_refused() helper (was stdlib 501 + HTML); smoke_test.py write-refusal section loops POST/PUT/DELETE/PATCH asserting 405 + application/json + documented error body (16 new asserts); pytest 469, smoke 70 checks, ruff/mypy clean (judge PASS 4491a3e7) | Low | 2±1 | c976378 | DS-V4-Flash |
| E2E-002 | Port-collision + false relay health — startup occupant detection (cachepilotd + dashboard backend fail with actionable errors naming the conflict + override; exit 2; defaults unchanged per PRD §26), relay probe now HTTP-confirmed via dedicated control endpoint `GET /cachepilot/health` on the relay (distinctive JSON body; only GET on that one path intercepted, other methods still pass through — PRD §27 deviation, pinned by test_health_control.py; relay stays transparent), `cachepilot status` + `/api/status` report healthy/occupied-by-another-service/unreachable, docs/dashboard.md Stock-Hermes port-overlap section (8787 ↔ HERMES_WEBUI_PORT hermes-webui/api/config.py:51, 8788 ↔ mcp serve) + relay-health semantics table; pytest 469 (+6), smoke 59 (+7), ruff/mypy clean (judge PASS 5212715d) | Medium | 3±1 | 8057569 | DS-V4-Flash |
| P12 | Optional UI dashboard — `dashboard/` (repo root, yarn 4 + React/TS + Vite): live leases, cache topology, cost graph, TTL learning, route changes, miss explanation (+status/churn views); read-only GET-only backend (`ReadOnlyTelemetryStore`, sqlite `mode=ro`, 405 on writes, byte-identical DB proof); never-fabricate empty states; provider_summary moved into base TelemetryStore (additive read-only, no core→dashboard dep); docs/dashboard.md runbook; README section; smoke_test 52 checks (judge PASS a3388463) | Low | 4±1 | ffe8e06 (+432205e) | DS-V4-Flash |
| CI-001 | GitHub Actions CI per PRD §140 — ci.yml (ruff, mypy w/ --native-parser --python-version 3.12 [PEP-695 aliases in adapters.py], pytest 463, coverage ≥90% [93.15], uv audit; race/integration tests in suite) + compat.yml Hermes matrix (PyPI latest-release + git-main current-main via editable_mode=compat [strict mode hides top-level registration_lifecycle → ModuleNotFoundError was install artifact, not drift], HERMES_AGENT_DIR set so stock-hermes-unchanged runs, entry-point group assert, weekly cron drift job); 13 pre-existing mypy errors fixed (classifier Literals ×10, 3 relay tests); mypy+pytest-cov dev deps; CONTRIBUTING CI docs (judge PASS 9129a7f0) | High | 2±1 | 91fc073 (+b456be2, +8554a00, +19dd70b) | DS-V4-Flash |
| P11 | Advanced optimizations (only after measurement) — empirical survival model P(cache survives \| age) from clean ttl_observations (PRD §99, KM estimator, honest horizon), cross-request prefix topology measurement view (per-layer stability/change frequency/est. prefix-token loss + per-route tool-schema ordering stability via tools_set_hash, `cachepilot topology`), volatile prompt isolation (system-suffix churn classified as volatile-value cause, DETECT-only), tool ordering measured NOT reordered; storage migration + relay observation feed; docs/advanced-optimizations.md (judge PASS 24f3f6cb) | Low | 5±1 | 2df919d | DS-V4-Flash |
| DOCS-000 | Documentation gate — Launchpad-style README (badges/nav/diagram/tables, no stale claims), CONTRIBUTING.md, SECURITY.md, LICENSE (Apache-2.0), docs/cache-economics.md + threat-model.md + hermes-integration.md runbooks, AGENTS.md Key Files touch-up (judge PASS 88cab1cf) | High | 3±1 | 6f624ee | DS-V4-Flash |
| P10 | Churn intelligence — layered prefix hashing (§24), system/tools/cache-key/route/history-boundary diff classification (§25, DETECT-only), likely cause + confidence, `cachepilot churn` + `explain-miss` CLIs, CACHEPILOT_CHURN_DETECTION_ENABLED flag (§164), churn_events schema migration (judge PASS 9241ec69) | Medium | 3±1 | 2bc4986 | DS-V4-Flash |
| P00 | Research harness — fake provider (KV cache/TTL/usage/pricing simulator), canonical request repr, usage normalization, cache fingerprint, economic calculator; all offline-testable | Critical | 5±1 | 4d8017c | DS-V4-Flash |
| P01 | Hermes plugin skeleton — manifest, middleware registration, lifecycle hooks, structured debug logs; CI test asserts stock Hermes unchanged (judge PASS ddfc8a00) | Critical | 3±1 | f423a54 (+2795b4b) | DS-V4-Flash |
| P02 | Long-task runtime — terminal long-task classifier (deterministic), auto-background promotion, completion notifications, subagent target tracking (refcount), command duration history; NO warming; benchmark polling reduction vs stock (judge PASS c85a2dad) | Critical | 5±1 | 4ff3a49 | DS-V4-Flash |
| P03 | Relay pass-through — cachepilotd (127.0.0.1:8787), 100% pass-through, 0 cache modification; golden differential tests: same response/stream/tools/errors, byte-identical bodies via aiter_raw, RFC 7230 hop-by-hop stripping, wildcard-bind refused (judge PASS 310803ad) | High | 4±1 | ce49ad1 | DS-V4-Flash |
| P04 | Physical request observation — correlation IDs (X-CachePilot-Session/Request/Turn, stripped before upstream), request+cache fingerprints from the physical HTTP request, usage parsing, cache telemetry (CONFIRMED_HIT/MISS_REBUILT/SUCCESS_UNVERIFIED/FAILED), route extraction, SQLite WAL telemetry store, `cachepilot status/leases/costs` CLI (judge PASS d48f7976) | High | 4±1 | c53eb82 | DS-V4-Flash |
| P05 | Lease manager — CacheLease/LeaseState (PRD §49), arm/invalidate/complete, generation counter, normal-request-reset, per-cache-identity locks, §53 deadline + §54 jitter scheduler, DRY-RUN only (`WOULD WARM IN 47s`, no network warm), leases table + real `cachepilot leases` listing, X-CachePilot-Targets header bridging plugin targets → relay, race tests (judge PASS a7f7f161) | High | 5±1 | b90d98b | DS-V4-Flash |
| P06 | Cache warming — bounded cache-equivalent replay: adapters.py (CacheCapabilities, CacheProviderAdapter base, OpenAICompatibleAdapter — max_tokens=1-family bounding, never invents fields), memory-only request snapshots (PRD §30), real warm executor via injectable transport (dry_run stays default), content discarded, warm_count/warm_cost_usd recorded, last_cache_touch_at only on verified touch, 2-miss warm circuit breaker, tool_choice policy §33, docs/provider-adapters.md, fake-provider + race tests (judge PASS 78483b62) | High | 5±1 | 8d6d81d | DS-V4-Flash |
| P07 | Economic controller — real economic gate replaces placeholder (PRD §134): estimated cold/cached resume costs populated from pricing (PRD §65 resolver), next-warm cost predictor, §63 resume probability (0.95 notify_on_complete / 0.20 detached / 0.0 no targets), budget_ratio 0.70 warm budget, ECONOMIC_STOP on budget exhaustion, CACHEPILOT_ECONOMICS_* config, WarmDecision explainability (PRD §145), economics.enabled=false restores watchdog; lease-level PRD §103 tests (judge PASS 6c486f48) | Critical | 4±1 | 96a35fa | DS-V4-Flash |
| P08 | TTL learning — route-aware learned bounds (lower/upper/estimate/confidence), observational refinement, `cachepilot ttl`; route changes must not corrupt bounds. TTLProfile per PRD §55, estimate §57, confidence §58, override hierarchy §59 (force > learned ≥0.7 > adapter hint > default), provider_profiles + ttl_observations tables §82, relay observation feed, lease TTL resolution wired (judge PASS 4a29492b) | High | 4±1 | c4d9b47 | DS-V4-Flash |
| P09 | Route intelligence — RouteIdentity model (PRD §71: gateway/upstream/endpoint/region/deployment, only observable fields), UC-5 router-miss analysis (ROUTE_INSTABILITY vs SHORT_TTL vs CLEAN, route-change events, instability miss never reaches TTL bounds), optional economic route affinity (§73-74: lease-scoped/temporary/reversible, savings-vs-cost gate, CACHEPILOT_ROUTE_AFFINITY default off, applied only when adapter can_pin_route), `cachepilot routes` CLI with instability stats (judge PASS 720baed0) | Medium | 4±1 | 445c440 | DS-V4-Flash |
| P-BOOT | Bootstrap — repo, AGENTS.md, docs/PRD.md (full spec), task board, gitreins, hilo, scheduler registration | Critical | 2±1 | — | DS-V4-Flash |

## Tick Log

- **2026-08-16 (work tick — E2E-009, 2a56880)**: Board had exactly one real
  pending task (E2E-009, Low) plus perpetual fixtures (E2E-001 Run 7 done last
  tick, not due; NEVER-DONE) → picked E2E-009 + dispatched worker (DS-V4-Flash
  /deepseek-v4-flash-0731 @ openrouter). Worker closed the wrong-schema SQLite
  honest-empty continuity gap: `_is_readable_sqlite()` in churn.py + server.py
  now verifies the EXPECTED CachePilot schema (sqlite_master count over
  request_events/churn_events/leases/provider_profiles/ttl_observations/
  route_events on the scratch mode=ro conn) in addition to PRAGMA quick_check;
  `_print_empty_store_notice(path)`; cmd_churn/cmd_explain_miss wrap SELECTs in
  try/except sqlite3.Error → notice + honest empty + exit 0 (defensive layer
  for probe/read races); server.py `_api` catches sqlite3.Error → rebuild
  empty → HTTP 200 empty JSON never 500; smoke_test wrong-schema case (28
  asserts, all 8 CLI read commands exit 0 + stderr + no traceback, /api/* 200
  empty); docs/dashboard.md wrong-schema bullet. Independent verification
  (trust-but-verify, not just worker claim): **482 pytest pass in 43.16s**,
  **ruff clean**, **smoke_test PASS** (incl. wrong-schema section), manual
  repro of wrong-schema DB → CLI exit 0 + stderr naming path + real honest
  empty output. GitReins lifecycle: create E2E-009 → start → worker `gitreins
  task complete` → judge PASS (verdict 1a96c257, tier1 lint/secrets/tests
  green) → task deleted, store clean (0 pending). MANDATORY PUSH: pushed
  2a56880, remote HEAD verified (unpushed=0, behind=0). CI: E2E-009 commit run
  in_progress at tick end, prior 2 runs success, no failures (the one run-7
  ruff-I001 failure was fixed in 377003c). Board updated (E2E-009 → Completed,
  E2E-001 gained Run 8). Only perpetual fixtures (E2E-001 not due, NEVER-DONE)
  remain → cooldown set 43200s (idle) via scheduler API PUT, verified
  (cooldown_s=43200, enabled=true).

- **2026-08-16 (work tick — E2E-001 Run 7, CLI/API)**: Board had the perpetual
  E2E-001 fixture (Run 6 was 449c76b, ~5 commits ago — due in the 5-10 window)
  plus a new Active E2E-009 finding. Dispatched the CLI/API E2E worker variant.
  Worker ran a full E2E verification tick: **fresh deploy** (`uv sync --group
  dev` clean; **482 pytest passed 40.34s**; `ruff check src/ packages/
  dashboard/backend/` All checks passed; `yarn build` 43 modules transformed;
  `dashboard/backend/smoke_test.py` SMOKE TEST PASSED). **Full user journey**
  (CLI/API only): live relay `cachepilotd` 9082 → mock upstream 9081 (pass-
  through GET/POST byte-identical; control `GET /cachepilot/health` distinctive
  JSON intercepted, HEAD/POST pass-through narrow PRD §27), live dashboard
  backend 9083 on a seeded telemetry DB (all 9 `/api/*` GET endpoints real
  JSON), all 8 CLI read commands consistent. **All 8 prior findings
  E2E-002..E2E-008 re-verified FIXED** (regression): E2E-002 relay readouts
  healthy/unreachable/occupied-by-foreign live + startup occupant detection;
  E2E-003/007 uniform JSON 405 for POST/PUT/DELETE/PATCH/OPTIONS/TRACE + HEAD
  405 JSON headers with 0 body bytes; E2E-004 missing `--db` never created, exit
  0, honest empty; E2E-005 status "route-change churn events 0"+footnote vs
  routes "route switches 1"; E2E-006 `@media (max-width:768px)` mobile collapse
  verified by code/build state; E2E-008 corrupt/non-SQLite DB → CLI exit 0 no
  traceback + dashboard 200 empty JSON. **1 new finding → E2E-009 (LOW)**:
  `_is_readable_sqlite` runs only PRAGMA quick_check (integrity, not schema), so
  a VALID SQLite file with an unrelated/wrong schema passes the probe and then
  crashes every CLI read (raw `sqlite3.OperationalError: no such table: ...`
  traceback, exit 1) and returns dashboard HTTP 500 — a continuity gap in
  E2E-008's honest-empty contract. Filed with exact repro (create unrelated-
  table DB → probe True → CLI traceback / dashboard 500), expected vs actual,
  and fix direction. Files changed (test-only): `e2e-output/report.md`,
  `e2e-output/tasks.md`, `.coding-hermes/tasks.md` (+ `e2e-output/run7/` notes +
  mock upstream test artifact). **No src/, packages/, or dashboard/backend/
  implementation modified.** Only e2e notes + board committed (worker commits
  findings; foreman runs the judge). Foreman layer: GitReins lifecycle — create
  E2E-001-R7 → start → worker pushed commit c5541d1 (verified remote
  HEAD, unpushed=0) → foreman judge run #1 returned **FAIL 4164a0c6** (Tier-2
  caught a genuine gap: full `ruff check .` reported I001 import-sort in the new
  `e2e-output/run7/mock_upstream.py`; worker's own lint had scoped to
  src/packages/dashboard and missed the artifact). Foreman fixed the 1-line
  mechanical lint issue directly (commit **377003c**, ruff `All checks
  passed!`), re-opened the task, and the re-run judge returned **Overall PASS
  68824bd**, all 4 criteria ✓ with live command evidence (482 pytest, ruff,
  yarn build 43 modules, smoke_test PASS; E2E-009 reproduced live, no source
  modified). Task E2E-001-R7 then deleted. Worker also created a new GitReins
  task `E2E-009` (4 criteria: extend quick_check probe to check expected tables
  via sqlite_master, wrap CLI reads so any sqlite3.Error → honest-empty exit 0,
  dashboard /api/* 200 empty JSON no 500, add smoke_test valid-but-wrong-schema
  case) — left in_progress as the next tick's mirror of board E2E-009. MANDATORY
  PUSH: c5541d1 + 377003c both pushed, remote HEAD verified (unpushed=0,
  behind=0). CI: prior 3 runs success, no failures. Board now has a real pending
  task E2E-009 → **project ACTIVE** → cooldown set 900s (already 900, verified
  via scheduler GET cooldown_s=900 enabled=true).

- **2026-08-15 (idle tick, b797659..HEAD — this tick)**: Board = only perpetual fixtures (E2E-001 Run 6 was done in E2E-008 work tick 449c76b, ~5 commits ago — window 5-10, not due; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, git clean tree, **upstream synced (unpushed=0, behind=0)**. Lightweight verification (board heavily audited, zero gaps, across prior idle+work ticks this cycle): **482 pytest pass in 58.71s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731). CHK8 CI: **4/4 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest idle-tick chore runs green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig (field is lowercase `cooldown_s`) — re-applied **43200s (idle)** via API PUT, verified GET (cooldown_s=43200, enabled=true).
- **2026-08-15 (idle tick, 8fcb096..HEAD)**: Board = only perpetual fixtures (E2E-001 Run 6 was 4 commits/ticks ago in E2E-008 work tick 449c76b — window 5-10, not due; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, **upstream synced (unpushed=0, behind=0)**, git clean tree. Lightweight verification (board heavily audited, zero gaps, across prior idle+work ticks this cycle): **482 pytest pass in 49.6s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS in prior ticks, model deepseek/deepseek-v4-flash-0731), DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified in prior tick). CHK8 CI: **3/3 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest idle-tick chore runs 5fe6558/d83a1b4/8fcb096 green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig (field is lowercase `cooldown_s`) — re-applied **43200s (idle)** via API PUT, verified GET (cooldown_s=43200, enabled=true).

- **2026-08-15 (idle tick, 5fe6558..HEAD)**: Board = only perpetual fixtures (E2E-001 Run 6 was done in the immediately-prior E2E-008 work tick 449c76b — 1 commit ago, window 5-10, not due; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, **upstream synced (unpushed=0, behind=0)**. Lightweight verification (board heavily audited, zero gaps, across prior idle+work ticks this cycle): **482 pytest pass in 65.4s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), git clean tree. CHK8 CI: **3/3 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest chore run 5fe6558 green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via API PUT (lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).
- **2026-08-15 (idle tick, 185e222..HEAD)**: Board = only perpetual fixtures (E2E-001 Run 6 was just done in the immediately-prior E2E-008 work tick 449c76b — 1 tick ago, window 5-10, not due; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, **upstream synced (unpushed=0, behind=0)**. Lightweight verification (board heavily audited, zero gaps, across prior idle+work ticks this cycle): **482 pytest pass in 45.5s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), git clean tree. CHK8 CI: **3/3 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest chore run 449c76b green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via API PUT (lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).
- **2026-08-15 (work tick — E2E-008, 449c76b)**: Board had exactly one real pending task (E2E-008, Low) → picked + dispatched worker (DS-V4-Flash/deepseek). Worker added `_is_readable_sqlite()` (PRAGMA quick_check on a scratch mode=ro conn) to both openers — `cachepilot_cli.churn.open_read_only_store` + `dashboard/backend/server.py open_store` — and mapped present-but-corrupt/non-SQLite DB onto the existing missing-file honest-empty path: all 8 CLI read commands exit 0 + stderr notice naming the path, no traceback; dashboard `/api/*` return 200 empty JSON, no 500. Also extended smoke_test.py with a corrupt-DB case (exit 0, empty JSON, stderr notice, no sqlite traceback) and updated docs/dashboard.md. Independent verification: **482 pytest pass in 43.55s**, **ruff clean**, **mypy clean (74 files)**, **smoke_test PASS** (incl. corrupt-DB checks). GitReins lifecycle: create E2E-008 → start → worker `gitreins task complete` (PASS verdict 4016f0be) → my foreman judge run first returned spurious FAIL 6d3146ea (tier1 `ruff: not found` — .venv/bin missing from judge PATH, pitfall #13) → re-run with `PATH=$PWD/.venv/bin` inline → **Overall PASS verdict 772922d8**, all 4 criteria ✓ → task deleted, store clean. MANDATORY PUSH: pushed 449c76b, remote HEAD verified (unpushed=0). CI: prior 3 runs success, no failures. Board updated (E2E-008 → Completed; E2E-001 row gained Run 6). Only perpetual fixtures (E2E-001, NEVER-DONE) remain → cooldown set 43200s (idle) via scheduler API PUT, verified (cooldown_s=43200, enabled=true).

- **2026-08-15 (idle tick, this session)**: Board = only perpetual fixtures (E2E-001 Run 4 was last E2E-007 commit 42249bb — 7 commits ago, approaching the 5-10 tick window's upper bound, next run due 1-3 ticks out; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, unpushed=0 / behind=0 (upstream synced). Lightweight verification (board heavily audited, zero gaps, in prior idle ticks this cycle): **482 pytest pass in 56s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), git clean tree. CHK8 CI: **3/3 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest idle-tick chore runs green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via raw API PUT (lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).

- **2026-08-15 (idle tick, this session)**: Board = only perpetual fixtures (E2E-001 Run 4 was last E2E-007 commit 42249bb, 1 tick ago — not due, window 5-10; NEVER-DONE audit). No real pending work → idle. Lightweight verification (board heavily audited, zero gaps, in prior idle ticks this cycle): **482 pytest pass in 97s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), git clean tree, **upstream synced (unpushed=0, behind=0)**, gitreins task store 0 pending. CHK8 CI: **3/3 recent runs success** (gh run list fresh — all idle-tick chore runs, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via API PUT (lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).

- **2026-08-15 (idle tick, 85d7425..HEAD)**: Board = only perpetual fixtures (E2E-001 Run 4 was 2026-08-15 via E2E-007 fix — not due, window 5-10; NEVER-DONE audit). No real pending work → idle. Git-history cross-ref + GitReins dual-source check: **gitreins task store 0 pending**, no committed work behind pending board tasks, unpushed=0 / behind=0 (upstream synced). Lightweight verification (board heavily audited, zero gaps, in prior idle ticks this cycle): **482 pytest pass in 56s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified), git clean tree. CHK8 CI: **4/4 recent runs success** (gh run list fresh, carterlasalle/cachepilot — latest idle-tick runs green, no failures). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via API PUT (lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).

- **2026-08-15 (idle tick, HEAD~1..HEAD)**: Board = only perpetual fixtures (E2E-001 Run 4 was last commit 42249bb, 1 tick ago — not due, window 5-10; NEVER-DONE audit). No real pending work → idle. Lightweight verification (board heavily audited, zero gaps, in prior 5 idle ticks this cycle): **482 pytest pass in 63s**, **ruff check src/ dashboard/backend/ All checks passed**, gitreins judge configured (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731), DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified), git clean tree, **upstream synced (unpushed=0, behind=0)**, gitreins task store 0 pending. CHK8 CI: **3/3 recent runs success** (gh run list fresh — board E2E-007 complete, E2E-007 feat all success). Zero new tasks found. Scheduler cooldown had reverted 43200→900 by daemon ApplyFleetConfig — re-applied **43200s (idle)** via API PUT (field is lowercase `cooldown_s`), verified GET (cooldown_s=43200, enabled=true).

- **2026-08-15 (work tick — E2E-007, 42249bb)**: Board had exactly one real pending task (E2E-007, Low) → picked + dispatched worker (DS-V4-Flash). Worker implemented uniform 405 contract: do_OPTIONS/do_TRACE → _write_refused() (JSON 405), do_HEAD → _write_refused(body=False) omits body; smoke_test write-refusal loop extended 4→7 methods (HEAD with empty-body assert); docs/dashboard.md contract made uniformly machine-readable. Independent verification: 482 pytest pass, ruff clean, mypy clean, smoke_test PASS. GitReins: create E2E-007 → start → worker `gitreins task complete` (first run spurious INCOMPLETE — iteration cap, pitfall #10; re-run with `PATH=$PWD/.venv/bin:$PATH` → COMPLETE, all 4 criteria PASS, verdict 62a94533) → task deleted, store clean. Pushed 42249bb, remote HEAD verified (unpushed=0). CI: E2E-007 commit run success (compat still in_progress), prior 3 success, no failures. Board updated (E2E-007 → Completed). Only perpetual fixtures (E2E-001, NEVER-DONE) remain → cooldown set 43200s (idle) via API PUT, verified (cooldown_s=43200, enabled=true; had reverted to 900 by daemon — re-applied fresh).

- **2026-08-15 (work tick — E2E-001 Run 3, 064c098)**: Board was idle but E2E-001 was DUE (8 idle ticks since Run 2, window 5-10) → picked E2E-001. Dispatch worker (DS-V4-Flash CLI/API E2E variant, no browser): fresh deploy, full user journey. Gates all green: **482 pytest pass, ruff clean, yarn build ✓, smoke_test.py PASS**. Re-verified E2E-002 (relay HTTP-confirmed probe, occupied/unreachable/healthy), E2E-003 (all 4 write methods 405 JSON), E2E-004 (missing --db no stray file), E2E-005 (route-change churn disambiguation), E2E-006 (responsive, via code state) — ALL FIXED, no longer reproduce. **1 new finding → E2E-007 (LOW)**: dashboard backend HEAD/OPTIONS/TRACE still return 501 + text/html, incomplete continuation of E2E-003; smoke_test only asserts 4 write methods so gate-invisible. E2E-007 filed as Active task for next tick. gitreins E2E-001 lifecycle: create→start→(worker commit 064c098)→complete (judge running). Cooldown left at 900s (active — E2E-007 needs a worker next tick).
- **2026-08-15 (idle tick, 194701b..HEAD)**: NEVER-DONE 14-point audit — CHK1 SPEC (PRD.md 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README badges/nav/diagram/tables, 8 runbooks, CONTRIBUTING, SECURITY, LICENSE, AGENTS.md) PASS; CHK3 tests (**482 pytest pass in 58s, 36 test files**; src/cachepilot = __init__-only anchor, 0 untested packages) PASS; CHK4 deps — `uv pip list --outdated --python .venv/bin/python3` shows only patch bumps (mypy 2.3.1, ruff 0.16.3, uvicorn 0.52.3) + pydantic-core 2.48.0 NOT actionable (pydantic 2.13.4 pins core==2.46.4, verified prior ticks — upgrade alone = SystemError) → no DEPS task; CHK5 pitfalls (**ruff check src/ dashboard/backend/ All checks passed**, only TODO hit is test_classifier.py:74 a literal-string grep fixture, 0 stubs/NotImplementedError) PASS; CHK6 perf (1 benchmark, fast) PASS; CHK7 endpoints (dashboard/backend/smoke_test.py 25.8K present — E2E-002/003/004 contract checks) PASS (smoke run covered in prior tick, suite unchanged); CHK8 CI (**3/3 recent runs success** — gh run list fresh, carterlasalle/cachepilot) PASS; CHK9 DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified) PASS; CHK10 quality (.gitignore complete, clean tree) PASS; CHK11 wiring (CLI/relay/dashboard all wired) PASS; CHK12 usability (E2E-001 R2 2026-08-13 browser-confirmed) PASS; CHK13 E2E (last run 2026-08-13 R2, only 4 ticks since — next in 5-10 ticks, not yet due) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731) PASS; gitreins task store: 0 pending. Zero new tasks found. Cooldown re-applied → 43200s (idle) via API PUT, verified GET (cooldown_s=43200, enabled=true; had reverted to 900 by daemon ApplyFleetConfig — re-applied fresh tick).
- **2026-08-15 (idle tick, 2a40379..HEAD)**: NEVER-DONE 14-point audit — CHK1 SPEC (PRD.md 228 headings, 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README badges/nav/diagram/tables, 8 runbooks, CONTRIBUTING, SECURITY, LICENSE, AGENTS.md) PASS; CHK3 tests (**482 pytest pass in 41s, 36 test files**; src/cachepilot = __init__-only anchor, 0 untested packages) PASS; CHK4 deps — `uv pip list --outdated --python .venv/bin/python3` shows only patch bumps (mypy 2.3.1, ruff 0.16.3, uvicorn 0.52.3) + pydantic-core 2.48.0 NOT actionable (pydantic 2.13.4 pins core==2.46.4, verified prior ticks — upgrade alone = SystemError) → no DEPS task; CHK5 pitfalls (**ruff check src/ dashboard/backend/ All checks passed**, 0 TODO/FIXME/HACK/XXX in src/, 0 stubs) PASS; CHK6 perf (1 benchmark, fast) PASS; CHK7 endpoints (**dashboard/backend/smoke_test.py PASS** — dashboard read-only contract, relay health readout, startup occupant detection, byte-identical read-only DB all green) PASS; CHK8 CI (**3/3 recent runs success** — gh run list fresh, carterlasalle/cachepilot) PASS; CHK9 DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified) PASS; CHK10 quality (.gitignore complete, clean tree) PASS; CHK11 wiring (CLI/relay/dashboard all wired) PASS; CHK12 usability (E2E-001 R2 2026-08-13 browser-confirmed) PASS; CHK13 E2E (last run 2026-08-13 R2, only 3 ticks since — next in 5-10 ticks, not yet due) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731) PASS; gitreins task store: 0 pending. Zero new tasks found. Cooldown re-applied → 43200s (idle) via API PUT, verified GET (cooldown_s=43200, enabled=true; had reverted to 900 by daemon restart — re-applied fresh this tick).
- **2026-08-15 (idle tick, 62bea18..HEAD)**: NEVER-DONE 14-point audit — CHK1 SPEC (PRD.md 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README badges/nav/diagram/tables, 8 runbooks, CONTRIBUTING, SECURITY, LICENSE, AGENTS.md) PASS; CHK3 tests (**482 pytest pass in 41s, 36 test files**; src/cachepilot = __init__-only anchor, 0 untested packages) PASS; CHK4 deps — `uv pip list --outdated --python .venv/bin/python3` shows only patch bumps (mypy 2.3.1, ruff 0.16.3, uvicorn 0.52.3) + pydantic-core 2.48.0 NOT actionable (pydantic 2.13.4 pins core==2.46.4, verified prior ticks — upgrade alone = SystemError) → no DEPS task; CHK5 pitfalls (0 TODO/FIXME/HACK/XXX in src/, 0 stubs) PASS; CHK6 perf (1 benchmark, fast) PASS; CHK7 endpoints (**smoke_test.py PASS** — dashboard read-only contract, relay health readout, startup occupant detection all green) PASS; CHK8 CI (**3/3 recent runs success** — gh run list fresh, carterlasalle/cachepilot) PASS; CHK9 DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified) PASS; CHK10 quality (.gitignore complete, clean tree) PASS; CHK11 wiring (CLI/relay/dashboard all wired) PASS; CHK12 usability (E2E-001 R2 2026-08-13 browser-confirmed) PASS; CHK13 E2E (last run 2026-08-13 R2, only 3 ticks since — next in 5-10 ticks, not yet due) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731) PASS; mypy re-verified clean under authoritative CI invocation `uvx mypy --python-executable .venv/bin/python --native-parser --python-version 3.12 --follow-imports=skip src packages` (Success, 74 files) — the 7-error report from running `src/ dashboard/backend/` together is an invocation artifact (cachepilot_core seen as installed, same file under two module names). gitreins task store: 0 pending. Zero new tasks found. Cooldown re-applied → 43200s (idle) via API PUT, verified GET (cooldown_s=43200, enabled=true; had reverted to 900 by daemon restart — re-applied fresh this tick).
- **2026-08-15 (idle tick, 4401a60..HEAD)**: NEVER-DONE 14-point audit — CHK1 SPEC (PRD.md 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README badges/nav/diagram/tables, 8 runbooks, CONTRIBUTING, SECURITY, LICENSE, AGENTS.md) PASS; CHK3 tests (**482 pytest pass in 59s, 36 test files**; src/cachepilot = __init__-only anchor, 0 untested packages) PASS; CHK4 deps — `pip list --outdated` empty, pydantic-core 2.46.4 pinned by pydantic 2.13.4 (import OK) → no DEPS task; CHK5 pitfalls (0 TODO/FIXME/HACK/XXX in src/, 0 stubs) PASS; CHK6 perf (1 benchmark, fast) PASS; CHK7 endpoints (**smoke_test.py PASS** — dashboard read-only contract, relay health readout, startup occupant detection all green) PASS; CHK8 CI (**3/3 recent runs success** — gh run list fresh, 2026-08-15) PASS; CHK9 DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified) PASS; CHK10 quality (.gitignore complete, clean tree) PASS; CHK11 wiring (CLI/relay/dashboard all wired) PASS; CHK12 usability (E2E-001 R2 2026-08-13 browser-confirmed) PASS; CHK13 E2E (last run 2026-08-13, E2E-006 fixed+judged; next run in 5-10 ticks) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731) PASS; gitreins task store: 0 pending. Zero new tasks found. Cooldown set → 43200s (idle) via API PUT, verified GET (cooldown_s=43200, enabled=true; prior 43200 had reverted to 900 by daemon restart — re-applied fresh this tick).
- **2026-08-14 (idle tick, 4401a60..HEAD)**: NEVER-DONE 14-point audit — CHK1 SPEC (PRD.md 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README badges/nav/diagram/tables, 8 runbooks, CONTRIBUTING, SECURITY, LICENSE, AGENTS.md) PASS; CHK3 tests (**482 pytest pass in 66s, 36 test files**; src/cachepilot anchor-only, 0 untested packages) PASS; CHK4 deps — `uv pip list --outdated --python .venv/bin/python3` empty, pydantic-core 2.46.4 pinned by pydantic 2.13.4 (verified import OK) → no DEPS task; CHK5 pitfalls (0 TODO/FIXME/HACK/XXX in project source, 0 stubs, gitleaks detect --no-git: no leaks) PASS; CHK6 perf (1 benchmark, project fast) PASS; CHK7 endpoints (smoke_test.py PASS 70+ checks incl. read-only contract, occupant detection) PASS; CHK8 CI (**3/3 recent runs success** — gh run list fresh) PASS; CHK9 DuckBrain namespace `cachepilot` healthy (3 keys: overview/pitfalls/e2e-history, list_keys verified) PASS; CHK10 quality (.gitignore complete, clean tree, largest file 1312 lines) PASS; CHK11 wiring (CLI cachepilot_cli, relay cachepilot_relay, dashboard backend all wired) PASS; CHK12 usability (E2E-001 R2 2026-08-13 browser-confirmed) PASS; CHK13 E2E (last run 2026-08-13, E2E-006 fixed+judged; next in 5-10 ticks) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS, model deepseek/deepseek-v4-flash-0731) PASS; gitreins task store: 0 pending. Zero new tasks found. Cooldown set → 43200s (idle), verified via scheduler GET.
- **2026-08-13 (idle tick, 8a565c0..HEAD)**: NEVER-DONE 14-point audit — CHK1 spec (PRD.md 169§ authoritative, no specs/ by design) PASS; CHK2 docs (README/CONTRIBUTING/SECURITY/AGENTS + 8 runbooks) PASS; CHK3 tests (**482 pytest pass in 43s, 37 test files**; src/cachepilot root = __init__-only anchor, no testable code) PASS; CHK4 deps — pydantic-core 2.48.0 NOT actionable (pydantic 2.13.4 pins core==2.46.4, verified import OK; upgrade alone = SystemError), ruff 0.16.3 patch bump (dev dep, no action), uvicorn 0.52.3 not a project dep (smoke_test dev import only) → no DEPS task; CHK5 pitfalls (0 TODO/FIXME/HACK/XXX in project code — all hits node_modules vendored, 0 stubs) PASS; CHK6 perf PASS; CHK7 endpoints (smoke_test.py full suite PASS, 70+ checks incl. E2E-002/003 sections) PASS; CHK8 CI (**3/3 recent runs success** — gh run list fresh) PASS; CHK9 DuckBrain — namespace `cachepilot` healthy, 3 keys (overview/pitfalls/e2e-history) list_keys verified PASS; CHK10 quality (.gitignore complete, clean tree) PASS; CHK11 wiring (CLI main, dashboard, relay all live) PASS; CHK12 usability (E2E-001 R2 browser 2026-08-13: console clean, 320/768/1280 visual) PASS; CHK13 E2E (2 runs this cycle, E2E-006 fixed+judged PASS; next run in 5-10 ticks) PASS; CHK14 gitreins judge (check-gitreins-judge.py PASS). Zero new tasks. Cooldown → 43200s (idle), verified via scheduler GET.

## [ ] NEVER-DONE — Run coding-hermes-never-done 14-point audit

Load coding-hermes-never-done skill. Run ALL checks: spec alignment, doc
coverage, test gaps, package upgrades, pitfall hunt, performance audit,
endpoint verification, CI/CD health, DuckBrain sync, code quality,
middle-out wiring, usability smoke test, E2E testing, GitReins judge.
Create a task for EVERY gap found. This task is never complete — the audit
always finds something.
